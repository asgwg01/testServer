http_server:
  addres: "0.0.0.0:8090"
  timeout: 4s
  idle_timeout: 60s

storage:
  storage_host: "localhost"
  storage_user: "postgres"
  storage_password: "0000"

swagger:
  run_swagger_ui: true
  swagger_url: "/swagger/"
